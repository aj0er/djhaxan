/**
 * Loaded sounds. This is used as a cache since the image/audio are large Base64-encoded strings and shouldn't be present multiple times.
 */
export let loadedSounds = {
    sounds: {}
};

/**
 * Global icons for use with custom items.
 */
export let globalIcons = {
    "SCRIPT": "/code.svg"
};